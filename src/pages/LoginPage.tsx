import React from 'react'
import { LoginForm } from '../features/auth/LoginForm'

export const LoginPage: React.FC = () => {
  return (
    <div>
      <h1>Login</h1>
      <LoginForm />
    </div>
  )
}
